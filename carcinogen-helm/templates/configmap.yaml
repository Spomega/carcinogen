apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.Chart.Name}}-config
  labels:
    name: {{.Chart.Name}}
data:
  # kv format
  key: "value"
  $actual-config-file$: "name.properties"

  # file format
  name.properties: |
    field=value1,value2

{% extends 'base.html.twig' %}

{% block title %}Post - {{ post.title }}{% endblock %}

{% block bread %}
    <div class="text-sm text-gray-400 dark:text-gray-500">
        <a href="{{ path('app_micro_post') }}">Home</a> / {{ post.title }}
    </div>
{% endblock %}

{% block body %}
    {{ include('micro_post/_post.html.twig',{ post: post }) }}
{% endblock %}